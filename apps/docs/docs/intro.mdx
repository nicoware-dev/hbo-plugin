---
sidebar_position: 1
title: Home
slug: /
---

import DocsIndexCards from '@site/src/components/DocsIndexCards';

# HBO Plugin

**Hermes Business Operations Plugin** turns Hermes Agent into a practical operating system for commerce and business workflows — without building a separate SaaS app.

<DocsIndexCards />

## One-liner

> HBO Plugin is a Hermes-native business operations package: specialized agent profiles, workflows, a dashboard tab, signals and action queues with approval gates, and integrations to external tools.

## The problem

Most SMB commerce teams run operations across disconnected tools:

- Google Sheets and CRMs for leads
- WhatsApp, marketplaces, and bots for customer conversations
- Manual spreadsheets for follow-ups and outreach batches
- Slack or Telegram for notifications

Hermes can reason and act — but it needs **business context**, **workflows**, **UI for operators**, and **guardrails** before it can run real commercial operations.

## The solution

HBO Plugin extends Hermes itself through:

| Layer | What it provides |
|-------|------------------|
| **Plugin core** | 19 `hbo_*` tools, 15 bundled skills, business rules, file-backed workspace state |
| **Dashboard** | Business Ops tab — Overview (charts), Agents, Workflows, Leads, Actions, Signals, **Business**, Audit, Tool Bridges, Setup |
| **Profiles** | Sales Ops, Growth, and Ops Lead agent distributions (+ 3 profile-local playbooks) |
| **Skills** | Workflow guidance + Composio, NVIDIA, Stripe Link CLI integrations |

## Core operating loop

```text
Business data  →  Signals  →  Hermes workflows  →  Action proposals  →  Approve / reject  →  Audit log
```

1. **Signals** surface business events (missed follow-ups, bot QA flags, hot leads).
2. **Workflows** (`inbound_sales`, `outbound_growth`, `daily_ops_briefing`) scan state and propose actions.
3. **Operators** approve or reject in Hermes chat or the dashboard.
4. **Audit** records every decision for traceability.
5. **Tool bridge** executes approved actions against Gmail, Slack, CRM, and other connected apps when integrations are enabled.

See [How it works](./how-it-works) for the full walkthrough.

## Why this matters for businesses

- **No missed follow-ups** — signals and workflows keep hot leads and stale conversations visible
- **Safer agent execution** — humans approve before agents trigger external effects
- **Auditable business operations** — approvals, rejections, and runs logged in one workspace

HBO Plugin is an operating layer for Hermes — not a CRM replacement.

## Objectives

HBO Plugin helps Hermes answer:

- What happened in the business today?
- Which leads or conversations need attention?
- What should each specialized agent do next?
- Which actions require human approval before execution?
- What was approved, rejected, and when?

Read [Objectives & scope](./objectives) for product goals, target users, and scope.

## Quick start

**Fastest path:** copy the [install prompt](./install#prompt) into Hermes — it installs the plugin, profiles, and verifies the dashboard.

**Manual path:** follow [Option B — Manual install](./install#option-b--manual-install) on the same page.
