# ops-lead

Operational leadership domain skill bundled with HBO Plugin.

## Requirements

- HBO Plugin enabled

## Register

Loaded as `hbo-plugin:ops-lead` when plugin is enabled.

## Quick use

Workspace snapshot → briefing → pending action queue → user approves in dashboard.

## Files

- `SKILL.md`, `examples/`, `evals/`, `skillopt-benchmark.jsonl`
